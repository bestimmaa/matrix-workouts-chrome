{
  "name": "full-matrix-workouts",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Chrome extension that replaces the Matrix workout dashboard with the full recorded telemetry",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
